<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ProjectTAIdePlot" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:colorBackground">@color/white</item>

        <!-- Menghapus ActionBar atau Toolbar -->
        <item name="windowActionBar">false</item> <!-- Menonaktifkan ActionBar -->
        <item name="windowNoTitle">true</item>   <!-- Menghapus Title bar yang mungkin muncul -->

    </style>
    <!-- Base application theme. -->
    <style name="Base.Base.Theme.ProjectTAIdePlot" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>
</resources>